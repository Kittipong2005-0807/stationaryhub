# การปรับปรุงระบบลบรูปภาพ - สรุปการทำงาน

## ✅ ไฟล์ที่ปรับปรุงแล้ว

### 1. **API Route สำหรับลบสินค้า**

**ไฟล์:** `app/api/products/[id]/route.ts`

**การปรับปรุง:**

- เพิ่ม import `unlink`, `join`, `existsSync` จาก fs/promises และ fs
- เพิ่มฟังก์ชัน `deleteImageFile()` สำหรับลบไฟล์รูปภาพจาก PATH_FILE_URL
- ปรับ DELETE method ให้ลบรูปภาพก่อนลบข้อมูลสินค้า

**โค้ดหลัก:**

```typescript
// ฟังก์ชันลบไฟล์รูปภาพ
async function deleteImageFile(filename: string): Promise<boolean> {
  try {
    const pathFileUrl = process.env.PATH_FILE_URL || 'D:/stationaryhub';
    const filePath = join(pathFileUrl, filename);

    if (existsSync(filePath)) {
      await unlink(filePath);
      return true;
    }
    return false;
  } catch (error) {
    console.error('Error deleting image file:', error);
    return false;
  }
}

// ในส่วน DELETE method
if (existingProduct.PHOTO_URL) {
  await deleteImageFile(existingProduct.PHOTO_URL);
}
```

### 2. **Admin Products Page**

**ไฟล์:** `app/admin/products/page.tsx`

**การปรับปรุง:**

- เพิ่ม import `uploadProductImage`, `getProductImageUrl` จาก `/lib/image-utils`
- ปรับฟังก์ชัน `handleImageUpload` ให้ใช้ `uploadProductImage()` และส่ง
  `oldImageUrl` เพื่อลบรูปเก่า
- ปรับฟังก์ชัน `getImageUrl` ให้ใช้ `getProductImageUrl()` แทน

**โค้ดหลัก:**

```typescript
// อัพโหลดรูปใหม่ (ลบรูปเก่าอัตโนมัติ)
const result = await uploadProductImage(
  file,
  editingProduct ? formData.PHOTO_URL : undefined
);

// แสดงรูปภาพ
const getImageUrl = (photoUrl: string | null | undefined) => {
  return getProductImageUrl(photoUrl || '');
};
```

### 3. **Image Utils Library**

**ไฟล์:** `lib/image-utils.ts` (สร้างใหม่)

**ฟังก์ชันที่มี:**

- `uploadProductImage()` - อัพโหลดรูปใหม่ (ลบรูปเก่าอัตโนมัติ)
- `deleteProductImage()` - ลบรูปภาพโดยตรง
- `getProductImageUrl()` - สร้าง URL สำหรับแสดงรูป

### 4. **Upload API Route**

**ไฟล์:** `app/api/upload-product-image/route.ts`

**การปรับปรุง:**

- เพิ่มการรับ `oldImageUrl` จาก FormData
- เพิ่มการลบรูปเก่าก่อนอัพโหลดรูปใหม่
- เพิ่ม DELETE method สำหรับลบรูปภาพโดยตรง

## 🔧 การทำงานของระบบใหม่

### **เมื่อมีการแก้ไขสินค้า (Edit)**

1. ผู้ใช้เลือกรูปใหม่
2. ระบบส่ง `oldImageUrl` ไป API upload
3. API ลบรูปเก่าจาก PATH_FILE_URL อัตโนมัติ
4. API อัพโหลดรูปใหม่
5. ส่ง filename ใหม่กลับมา

### **เมื่อมีการลบสินค้า (Delete)**

1. API products/[id] รับคำสั่งลบ
2. ดึงข้อมูลสินค้าเพื่อเช็ค PHOTO_URL
3. ลบไฟล์รูปภาพจาก PATH_FILE_URL
4. ลบข้อมูลสินค้าจากฐานข้อมูล

### **การแสดงรูปภาพ**

- ใช้ `getProductImageUrl()` สำหรับสร้าง URL แบบสม่ำเสมอ
- รองรับการแสดงผ่าน `/api/image/[filename]`
- ใช้ PATH_FILE_URL เป็น root directory

## 📁 ไฟล์ที่ได้รับผลกระทบ

✅ **ปรับปรุงแล้ว:**

- `app/api/products/[id]/route.ts` - เพิ่มการลบรูป
- `app/admin/products/page.tsx` - ใช้ image utils
- `lib/image-utils.ts` - สร้างใหม่
- `app/api/upload-product-image/route.ts` - รองรับการลบรูปเก่า

🔍 **ตรวจสอบแล้ว - ไม่ต้องปรับ:**

- `app/manager/products/page.tsx` - ไม่มีการลบสินค้า (แค่แสดงรายการ)
- Cart pages - การลบออกจากตะกร้าไม่ลบไฟล์รูป

## 🎯 ประโยชน์ที่ได้รับ

1. **ประหยัดพื้นที่เก็บข้อมูล** - รูปเก่าถูกลบอัตโนมัติ
2. **ความสะอาดของระบบ** - ไม่มีไฟล์รูปไร้เจ้าของ
3. **ความสอดคล้อง** - ใช้ utility functions เดียวกันทั้งระบบ
4. **การบำรุงรักษา** - โค้ดเป็นระเบียบและแยกหน้าที่ชัดเจน

## 🔄 วิธีการใช้งานใหม่

### สำหรับการแก้ไขสินค้า:

```typescript
// ไม่ต้องจัดการอะไรเพิ่ม - ระบบจะลบรูปเก่าอัตโนมัติ
const result = await uploadProductImage(newFile, currentImageUrl);
```

### สำหรับการลบสินค้า:

```typescript
// เรียก API DELETE /api/products/[id] - ระบบจะลบรูปอัตโนมัติ
const response = await fetch(`/api/products/${productId}`, {
  method: 'DELETE'
});
```

### สำหรับการแสดงรูป:

```typescript
// ใช้ function เดียวกันทั้งระบบ
const imageUrl = getProductImageUrl(product.PHOTO_URL);
```
