# 🔧 การแก้ไขปัญหาแจ้งเตือนซ้ำซ้อนเมื่อ Manager อนุมัติ

## 🚨 **ปัญหาที่พบ**

### **การส่งแจ้งเตือนซ้ำซ้อน**
- **Manager อนุมัติ requisition** → ได้รับแจ้งเตือนซ้ำซ้อน
- **User สร้าง requisition** → ได้รับแจ้งเตือนซ้ำซ้อน
- **Admin ได้รับแจ้งเตือนซ้ำซ้อน** เมื่อมีการอนุมัติ

## ✅ **สาเหตุของปัญหา**

### **1. การเรียกใช้ฟังก์ชันแจ้งเตือนซ้ำซ้อน**

#### **ในไฟล์ `approve/route.ts`**
```typescript
// ❌ ผิด - เรียกใช้ซ้ำ
const result = await ApprovalService.createApproval(approvalData)

// ส่งการแจ้งเตือนตามผลการอนุมัติ
if (action === "approve") {
  await NotificationService.notifyRequisitionApproved(requisitionId, userId) // ซ้ำ!
} else if (action === "reject") {
  await NotificationService.notifyRequisitionRejected(requisitionId, userId, note) // ซ้ำ!
}
```

#### **ในไฟล์ `approval-service.ts`**
```typescript
// ✅ ถูกต้อง - มีการส่งแจ้งเตือนอยู่แล้ว
const result = await prisma.$transaction(async (tx) => {
  // ... transaction logic
})

// ส่งการแจ้งเตือนหลังจาก transaction สำเร็จ
if (approvalData.STATUS === "APPROVED") {
  await NotificationService.notifyRequisitionApproved(approvalData.REQUISITION_ID, approvalData.APPROVED_BY)
} else if (approvalData.STATUS === "REJECTED") {
  await NotificationService.notifyRequisitionRejected(approvalData.REQUISITION_ID, approvalData.APPROVED_BY, approvalData.NOTE)
}
```

### **2. การเรียกใช้ฟังก์ชันแจ้งเตือนซ้ำซ้อนในการสร้าง Requisition**

#### **ในไฟล์ `requisitions/route.ts`**
```typescript
// ❌ ผิด - เรียกใช้ซ้ำ
const requisition = await prisma.rEQUISITIONS.create({...})
await NotificationService.notifyRequisitionCreated(requisition.REQUISITION_ID, empCode) // ซ้ำ!
```

#### **ในไฟล์ `orgcode3-service.ts`**
```typescript
// ✅ ถูกต้อง - มีการส่งแจ้งเตือนอยู่แล้ว
const finalRequisitionId = await this.createRequisitionWithSiteId(...)
await NotificationService.notifyRequisitionCreated(finalRequisitionId, userId)
```

### **3. การใช้ Type ผิดในการแจ้งเตือน Admin**

#### **ในไฟล์ `notification-service.ts`**
```typescript
// ❌ ผิด - ใช้ type ผิด
await this.logNotification({
  type: 'requisition_pending', // ผิด! ควรเป็น 'requisition_approved'
  userId: admin.USER_ID,
  requisitionId,
  message: `มีการอนุมัติคำขอเบิกใหม่...`
})
```

## 🔧 **การแก้ไขที่ทำ**

### **1. ลบการเรียกใช้ฟังก์ชันแจ้งเตือนซ้ำใน `approve/route.ts`**
```typescript
// ✅ หลังแก้ไข
const result = await ApprovalService.createApproval(approvalData)

// การแจ้งเตือนจะถูกส่งโดย ApprovalService.createApproval แล้ว
// ไม่ต้องส่งซ้ำที่นี่
```

### **2. ลบการเรียกใช้ฟังก์ชันแจ้งเตือนซ้ำใน `requisitions/route.ts`**
```typescript
// ✅ หลังแก้ไข
const requisition = await prisma.rEQUISITIONS.create({...})

// การแจ้งเตือนจะถูกส่งโดย OrgCode3Service.createRequisitionWithSiteId แล้ว
// ไม่ต้องส่งซ้ำที่นี่
```

### **3. แก้ไข Type ในการแจ้งเตือน Admin**
```typescript
// ✅ หลังแก้ไข
await this.logNotification({
  type: 'requisition_approved', // ถูกต้อง!
  userId: admin.USER_ID,
  requisitionId,
  message: `มีการอนุมัติคำขอเบิกใหม่...`
})
```

## 📋 **ไฟล์ที่แก้ไข**

### **1. `app/api/requisitions/[id]/approve/route.ts`**
- **ลบการเรียกใช้** `notifyRequisitionApproved` และ `notifyRequisitionRejected` ซ้ำ
- **เพิ่มคอมเมนต์** อธิบายว่าการแจ้งเตือนจะถูกส่งโดย `ApprovalService.createApproval` แล้ว

### **2. `app/api/requisitions/route.ts`**
- **ลบการเรียกใช้** `notifyRequisitionCreated` ซ้ำ
- **เพิ่มคอมเมนต์** อธิบายว่าการแจ้งเตือนจะถูกส่งโดย `OrgCode3Service.createRequisitionWithSiteId` แล้ว

### **3. `lib/notification-service.ts`**
- **แก้ไข type** จาก `'requisition_pending'` เป็น `'requisition_approved'` ในฟังก์ชัน `notifyAdmins`

## 🎯 **ผลลัพธ์ที่คาดหวัง**

### **✅ หลังแก้ไข**
- **Manager อนุมัติ** → ได้รับแจ้งเตือนเพียงครั้งเดียว
- **User สร้าง requisition** → ได้รับแจ้งเตือนเพียงครั้งเดียว
- **Admin ได้รับแจ้งเตือน** เพียงครั้งเดียวเมื่อมีการอนุมัติ
- **ไม่มีการส่งแจ้งเตือนซ้ำซ้อน** ในระบบ

### **✅ การทำงานที่คาดหวัง**
1. **User สร้าง requisition** → `OrgCode3Service.createRequisitionWithSiteId` ส่งแจ้งเตือน 1 ครั้ง
2. **Manager อนุมัติ** → `ApprovalService.createApproval` ส่งแจ้งเตือน 1 ครั้ง
3. **Admin ได้รับแจ้งเตือน** 1 ครั้งเมื่อมีการอนุมัติ
4. **ไม่มีการส่งแจ้งเตือนซ้ำซ้อน**

## 🚀 **การทดสอบ**

### **1. ทดสอบการสร้าง Requisition**
```bash
# สร้าง requisition ใหม่
curl -X POST /api/requisitions
# ตรวจสอบว่าได้รับแจ้งเตือนเพียงครั้งเดียว
```

### **2. ทดสอบการอนุมัติ Requisition**
```bash
# Manager อนุมัติ requisition
curl -X POST /api/requisitions/[id]/approve
# ตรวจสอบว่าได้รับแจ้งเตือนเพียงครั้งเดียว
```

### **3. ตรวจสอบ Log**
```bash
# ตรวจสอบ log ว่าไม่มีการเรียกใช้ฟังก์ชันแจ้งเตือนซ้ำ
grep "notifyRequisitionApproved" logs
grep "notifyRequisitionCreated" logs
```

## 📝 **สรุป**

การแก้ไขนี้ทำให้:
- **ไม่มีการส่งแจ้งเตือนซ้ำซ้อน** เมื่อ Manager อนุมัติ
- **ระบบทำงานได้อย่างถูกต้อง** และมีประสิทธิภาพ
- **User Experience ดีขึ้น** เพราะไม่ได้รับแจ้งเตือนซ้ำซ้อน
- **Performance ดีขึ้น** เพราะไม่มีการส่งแจ้งเตือนซ้ำ
