# ระบบประวัติราคาสินค้า

## ภาพรวม

ระบบประวัติราคาสินค้าถูกออกแบบมาเพื่อติดตามการเปลี่ยนแปลงราคาของสินค้าทั้งหมดในระบบ โดยจะบันทึกประวัติทุกครั้งที่มีการแก้ไขราคา

## ฟีเจอร์หลัก

### ✅ **การบันทึกประวัติราคาอัตโนมัติ**
- บันทึกประวัติราคาทุกครั้งที่มีการแก้ไขราคาในหน้า Product Management
- เก็บข้อมูลราคาเก่า, ราคาใหม่, การเปลี่ยนแปลง, และเปอร์เซ็นต์การเปลี่ยนแปลง
- บันทึกวันที่และผู้ที่ทำการเปลี่ยนแปลง

### ✅ **หน้าแสดงประวัติราคา**
- แสดงประวัติการเปลี่ยนแปลงราคาทั้งหมด
- ฟิลเตอร์ตามสินค้าและปี
- ค้นหาตามชื่อสินค้าหรือหมายเหตุ
- แสดงไอคอนและสีสำหรับการเปลี่ยนแปลงราคา

### ✅ **การคำนวณอัตโนมัติ**
- คำนวณการเปลี่ยนแปลงราคา (ราคาใหม่ - ราคาเก่า)
- คำนวณเปอร์เซ็นต์การเปลี่ยนแปลง
- แสดงไอคอนขึ้น/ลง/คงที่

## โครงสร้างฐานข้อมูล

### ตาราง `PRICE_HISTORY`
```sql
CREATE TABLE PRICE_HISTORY (
  HISTORY_ID        INT PRIMARY KEY IDENTITY(1,1),
  PRODUCT_ID        INT NOT NULL,
  OLD_PRICE         DECIMAL(18,2) NULL,
  NEW_PRICE         DECIMAL(18,2) NULL,
  PRICE_CHANGE      DECIMAL(18,2) NULL,
  PERCENTAGE_CHANGE DECIMAL(5,2) NULL,
  YEAR              INT NOT NULL,
  RECORDED_DATE      DATETIME DEFAULT GETDATE(),
  NOTES             VARCHAR(1000) NULL,
  CREATED_BY        VARCHAR(50) NULL,
  FOREIGN KEY (PRODUCT_ID) REFERENCES PRODUCTS(PRODUCT_ID)
)
```

## การทำงานของระบบ

### 1. **การบันทึกประวัติราคา**
เมื่อ admin แก้ไขราคาสินค้าในหน้า Product Management:

```typescript
// ใน app/api/products/[id]/route.ts
const oldPrice = currentProduct.UNIT_COST ? parseFloat(currentProduct.UNIT_COST.toString()) : 0
const newPrice = updateData.UNIT_COST !== undefined && updateData.UNIT_COST !== null && updateData.UNIT_COST !== '' ? parseFloat(updateData.UNIT_COST) : 0

// Record price history if price has changed
if (oldPrice !== newPrice) {
  const priceChange = newPrice - oldPrice
  const percentageChange = oldPrice > 0 ? (priceChange / oldPrice) * 100 : 0

  await prisma.pRICE_HISTORY.create({
    data: {
      PRODUCT_ID: productId,
      OLD_PRICE: oldPrice,
      NEW_PRICE: newPrice,
      PRICE_CHANGE: priceChange,
      PERCENTAGE_CHANGE: percentageChange,
      YEAR: new Date().getFullYear(),
      RECORDED_DATE: new Date(),
      NOTES: `Price updated by admin from ฿${oldPrice} to ฿${newPrice}`,
      CREATED_BY: user?.USERNAME || 'ADMIN',
    },
  })
}
```

### 2. **การแสดงประวัติราคา**
หน้า `/stationaryhub/admin/price-history` แสดงข้อมูลจาก API:

```typescript
// ใน app/api/products/price-history/route.ts
const priceHistory = await prisma.pRICE_HISTORY.findMany({
  where: whereConditions,
  include: {
    PRODUCTS: {
      include: {
        PRODUCT_CATEGORIES: true
      }
    }
  },
  orderBy: {
    RECORDED_DATE: 'desc'
  },
  take: parseInt(limit)
})
```

## API Endpoints

### 1. **GET /api/products/price-history**
ดึงข้อมูลประวัติราคา

**Query Parameters:**
- `productId` (optional): กรองตามสินค้า
- `year` (optional): กรองตามปี
- `limit` (optional): จำนวนรายการที่ต้องการ (default: 100)

**Response:**
```json
[
  {
    "HISTORY_ID": 1,
    "PRODUCT_ID": 1,
    "OLD_PRICE": 100.00,
    "NEW_PRICE": 120.00,
    "PRICE_CHANGE": 20.00,
    "PERCENTAGE_CHANGE": 20.00,
    "YEAR": 2025,
    "RECORDED_DATE": "2025-01-15T10:30:00.000Z",
    "NOTES": "Price updated by admin from ฿100 to ฿120",
    "CREATED_BY": "admin",
    "PRODUCTS": {
      "PRODUCT_NAME": "Ballpoint Pen",
      "PRODUCT_CATEGORIES": {
        "CATEGORY_NAME": "Writing Tools"
      }
    }
  }
]
```

### 2. **POST /api/products/price-history**
สร้างประวัติราคาใหม่

**Request Body:**
```json
{
  "productId": 1,
  "oldPrice": 100.00,
  "newPrice": 120.00,
  "notes": "Price increase due to supplier cost"
}
```

## การใช้งาน

### สำหรับ Admin:

1. **เข้าสู่หน้า Product Management**
   - ไปที่ `/stationaryhub/admin/products`

2. **แก้ไขราคาสินค้า**
   - คลิกปุ่ม Edit (✏️) ที่สินค้าที่ต้องการ
   - เปลี่ยนราคาในช่อง Unit Price
   - คลิกปุ่ม "Update Product"

3. **ดูประวัติราคา**
   - คลิกปุ่ม "Price History" ในหน้า Product Management
   - หรือไปที่ `/stationaryhub/admin/price-history` โดยตรง

4. **กรองข้อมูล**
   - ใช้ช่องค้นหาเพื่อหาสินค้าหรือหมายเหตุ
   - เลือกสินค้าเฉพาะจาก dropdown
   - เลือกปีเฉพาะจาก dropdown

## ตัวอย่างข้อมูล

### การเปลี่ยนแปลงราคา:
- **ราคาเพิ่มขึ้น**: ฿100 → ฿120 (+฿20, +20%)
- **ราคาลดลง**: ฿120 → ฿100 (-฿20, -16.7%)
- **ราคาคงที่**: ไม่มีการบันทึกประวัติ

### การแสดงผล:
- 🟢 **สีเขียว**: ราคาเพิ่มขึ้น
- 🔴 **สีแดง**: ราคาลดลง
- ⚪ **สีเทา**: ไม่มีการเปลี่ยนแปลง

## หมายเหตุ

- ระบบจะบันทึกประวัติเฉพาะเมื่อราคามีการเปลี่ยนแปลง
- ข้อมูลจะถูกเก็บในฐานข้อมูลและไม่สามารถลบได้
- การแสดงผลจะเรียงตามวันที่ล่าสุดก่อน
- ระบบรองรับการกรองและค้นหาข้อมูล
- ประวัติราคาจะถูกเก็บไว้ตลอดไปเพื่อการติดตามแนวโน้ม

## การพัฒนาต่อ

### ฟีเจอร์ที่อาจเพิ่มในอนาคต:
- 📊 **กราฟแสดงแนวโน้มราคา**
- 📈 **รายงานการเปลี่ยนแปลงราคา**
- 🔔 **การแจ้งเตือนเมื่อราคาเปลี่ยนแปลงมาก**
- 📋 **การส่งออกรายงาน**
- 🎯 **การเปรียบเทียบราคาระหว่างสินค้า**
