# 🗄️ การติดตั้งตาราง PRICE_HISTORY

## 📋 **ปัญหาที่แก้ไข**
เมื่ออัปเดตราคาสินค้าในตาราง `PRODUCTS` ราคาเก่าจะหายไป ทำให้ไม่สามารถติดตามประวัติการเปลี่ยนแปลงราคาได้

## 🚀 **วิธีแก้ไข**

### **ขั้นตอนที่ 1: สร้างตารางในฐานข้อมูล**

#### **ตัวเลือก A: ใช้ SQL Script (แนะนำ)**
1. เปิดไฟล์ `scripts/run-migration.sql`
2. เปลี่ยนชื่อฐานข้อมูลในบรรทัด `USE [your_database_name];`
3. รัน script ใน SQL Server Management Studio หรือ Azure Data Studio

#### **ตัวเลือก B: รันด้วย Prisma**
```bash
# อัปเดต Prisma schema
npx prisma db push

# หรือสร้าง migration
npx prisma migrate dev --name add_price_history
```

### **ขั้นตอนที่ 2: ตรวจสอบการติดตั้ง**

#### **ตรวจสอบตาราง**
```sql
-- ตรวจสอบว่าตารางถูกสร้างแล้ว
SELECT * FROM INFORMATION_SCHEMA.TABLES 
WHERE TABLE_NAME = 'PRICE_HISTORY';

-- ตรวจสอบโครงสร้างตาราง
SELECT * FROM INFORMATION_SCHEMA.COLUMNS 
WHERE TABLE_NAME = 'PRICE_HISTORY';
```

#### **ตรวจสอบ Foreign Key**
```sql
-- ตรวจสอบ Foreign Key
SELECT * FROM INFORMATION_SCHEMA.REFERENTIAL_CONSTRAINTS 
WHERE CONSTRAINT_NAME = 'FK_PRICE_HISTORY_PRODUCTS';
```

### **ขั้นตอนที่ 3: ทดสอบระบบ**

#### **ทดสอบการบันทึกประวัติราคา**
1. ไปที่หน้า Admin Dashboard
2. อัปเดตราคาสินค้า
3. ตรวจสอบว่าข้อมูลถูกบันทึกลงตาราง `PRICE_HISTORY`

#### **ทดสอบการดึงประวัติราคา**
```bash
# ทดสอบ API
GET /api/products/price-history-detail?productId=1&year=2025
```

## 📊 **โครงสร้างตาราง PRICE_HISTORY**

| Column | Type | Description |
|--------|------|-------------|
| HISTORY_ID | INT | Primary Key, Auto Increment |
| PRODUCT_ID | INT | Foreign Key to PRODUCTS |
| OLD_PRICE | DECIMAL(18,2) | ราคาเก่า |
| NEW_PRICE | DECIMAL(18,2) | ราคาใหม่ |
| PRICE_CHANGE | DECIMAL(18,2) | การเปลี่ยนแปลงราคา |
| PERCENTAGE_CHANGE | DECIMAL(5,2) | เปอร์เซ็นต์การเปลี่ยนแปลง |
| YEAR | INT | ปี |
| RECORDED_DATE | DATETIME | วันที่บันทึก |
| NOTES | VARCHAR(1000) | หมายเหตุ |
| CREATED_BY | VARCHAR(50) | ผู้เปลี่ยนแปลง |

## 🔧 **การทำงานของระบบ**

### **เมื่ออัปเดตราคาสินค้า:**
1. **บันทึกราคาเก่า**: เก็บราคาเก่าลงตาราง `PRICE_HISTORY`
2. **อัปเดตราคาใหม่**: เปลี่ยนราคาในตาราง `PRODUCTS`
3. **บันทึกประวัติ**: เก็บข้อมูลการเปลี่ยนแปลงทั้งหมด

### **การดึงประวัติราคา:**
- **API**: `/api/products/price-history-detail`
- **ข้อมูล**: ราคาเก่า, ราคาใหม่, การเปลี่ยนแปลง, เปอร์เซ็นต์
- **การเรียงลำดับ**: ตามวันที่ล่าสุด

## ✅ **ผลลัพธ์ที่ได้**

### **ก่อนแก้ไข:**
- ราคาเก่าหายไปเมื่ออัปเดต
- ไม่สามารถติดตามประวัติราคาได้
- ข้อมูลสถิติไม่ครบถ้วน

### **หลังแก้ไข:**
- เก็บประวัติราคาทั้งหมด
- สามารถติดตามการเปลี่ยนแปลงราคาได้
- ข้อมูลสถิติครบถ้วนและแม่นยำ

## 🚨 **ข้อควรระวัง**

1. **Backup ฐานข้อมูล** ก่อนรัน migration
2. **ทดสอบใน Environment ทดสอบ** ก่อน
3. **ตรวจสอบสิทธิ์** การสร้างตารางในฐานข้อมูล
4. **ตรวจสอบ Foreign Key** ว่าถูกต้อง

## 📞 **หากมีปัญหา**

1. ตรวจสอบ error log ใน console
2. ตรวจสอบการเชื่อมต่อฐานข้อมูล
3. ตรวจสอบสิทธิ์การเข้าถึงฐานข้อมูล
4. ติดต่อทีมพัฒนา

---

**🎯 หลังจากติดตั้งเสร็จ ระบบจะสามารถเก็บประวัติราคาได้ครบถ้วน และไม่สูญเสียข้อมูลราคาเก่าอีกต่อไป!**
