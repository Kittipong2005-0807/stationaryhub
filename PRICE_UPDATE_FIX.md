# การแก้ไขปัญหาการบันทึกราคา

## ปัญหาที่พบ

เมื่อ admin เปลี่ยนราคาสินค้าแล้วกดบันทึก ราคาไม่ถูกบันทึกลงฐานข้อมูล

## สาเหตุของปัญหา

### 1. การจัดการค่า `0` ใน API Routes
- ใน API routes เดิมใช้การตรวจสอบ: `body.UNIT_COST ? parseFloat(body.UNIT_COST) : null`
- ปัญหา: เมื่อ `UNIT_COST` เป็น `0` จะถูกประเมินเป็น `false` และบันทึกเป็น `null`

### 2. การจัดการค่า Empty String
- เมื่อผู้ใช้ลบค่าในช่องราคา จะได้ empty string (`""`)
- การแปลง `parseFloat("")` จะได้ `NaN`
- การใช้ `|| 0` จะทำให้ได้ `0` แต่ API route อาจไม่จัดการได้ถูกต้อง

## การแก้ไข

### 1. แก้ไข API Route สำหรับการสร้างสินค้าใหม่
**ไฟล์**: `app/api/products/route.ts`

```typescript
// เดิม
UNIT_COST: body.UNIT_COST ? parseFloat(body.UNIT_COST) : null,

// ใหม่
UNIT_COST: body.UNIT_COST !== undefined && body.UNIT_COST !== null && body.UNIT_COST !== '' ? parseFloat(body.UNIT_COST) : 0,
```

### 2. แก้ไข API Route สำหรับการอัปเดตสินค้า
**ไฟล์**: `app/api/products/[id]/route.ts`

```typescript
// เดิม
UNIT_COST: updateData.UNIT_COST ? parseFloat(updateData.UNIT_COST) : null,

// ใหม่
UNIT_COST: updateData.UNIT_COST !== undefined && updateData.UNIT_COST !== null && updateData.UNIT_COST !== '' ? parseFloat(updateData.UNIT_COST) : 0,
```

### 3. แก้ไขการจัดการค่าในหน้า Admin
**ไฟล์**: `app/admin/products/page.tsx`

```typescript
// เดิม
onChange={(e) => setFormData({ ...formData, UNIT_COST: parseFloat(e.target.value) || 0 })}

// ใหม่
onChange={(e) => setFormData({ ...formData, UNIT_COST: e.target.value === '' ? 0 : parseFloat(e.target.value) || 0 })}
```

## ผลลัพธ์

### ✅ **การแก้ไขที่ทำเสร็จแล้ว:**

1. **รองรับค่า `0`**: ราคา `0` จะถูกบันทึกเป็น `0` ไม่ใช่ `null`
2. **รองรับ Empty String**: เมื่อลบค่าในช่องราคาจะถูกแปลงเป็น `0`
3. **รองรับค่า Negative**: ยังคงรองรับค่าติดลบได้
4. **รองรับค่า Decimal**: ยังคงรองรับทศนิยมได้

### 🔧 **การทำงานใหม่:**

1. **ผู้ใช้ป้อนราคา `0`** → บันทึกเป็น `0` ในฐานข้อมูล
2. **ผู้ใช้ลบค่าในช่องราคา** → บันทึกเป็น `0` ในฐานข้อมูล
3. **ผู้ใช้ป้อนราคาปกติ** → บันทึกตามค่าที่ป้อน
4. **ผู้ใช้ไม่ป้อนราคา** → บันทึกเป็น `0` ในฐานข้อมูล

### 📊 **การทดสอบ:**

- ✅ ป้อนราคา `0` → บันทึกเป็น `0`
- ✅ ป้อนราคา `10.50` → บันทึกเป็น `10.50`
- ✅ ลบค่าในช่องราคา → บันทึกเป็น `0`
- ✅ ไม่ป้อนราคา → บันทึกเป็น `0`

## หมายเหตุ

- การแก้ไขนี้จะทำให้ราคา `0` ถูกบันทึกในฐานข้อมูลแทนที่จะเป็น `null`
- ในการแสดงผล ราคา `0` จะแสดงเป็น `฿0` แทนที่จะเป็น `-`
- การแก้ไขนี้ใช้ได้ทั้งการสร้างสินค้าใหม่และการแก้ไขสินค้า
