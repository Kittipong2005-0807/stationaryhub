@echo off
echo =====================================================
echo แก้ไขการตั้งค่า timezone ของ SQL Server ให้เป็น Thai time
echo =====================================================

echo.
echo 🔧 กำลังรัน SQL script เพื่อแก้ไข timezone...
echo.

REM รัน SQL script
sqlcmd -S "10.1.0.4" -d "StationaryNew" -U "kittipong" -P "password@1" -i "scripts/fix-sql-timezone.sql"

if %ERRORLEVEL% EQU 0 (
    echo.
    echo ✅ การแก้ไข timezone เสร็จสิ้น!
    echo.
    echo 🧪 กำลังทดสอบการตั้งค่าใหม่...
    node test-thai-timezone.js
) else (
    echo.
    echo ❌ เกิดข้อผิดพลาดในการรัน SQL script
    echo กรุณาตรวจสอบการเชื่อมต่อฐานข้อมูลและสิทธิ์การเข้าถึง
)

echo.
echo กด Enter เพื่อปิดหน้าต่าง...
pause > nul
