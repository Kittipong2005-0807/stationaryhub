# คู่มือการอัปโหลดรูปภาพสินค้า

## ระบบการอัปโหลดรูปภาพ

### 📁 โฟลเดอร์ที่ใช้เก็บรูปภาพ
- รูปภาพจะถูกเก็บในโฟลเดอร์: `public/` โดยตรง
- URL ที่เข้าถึงได้: `/stationaryhub/[filename]`

### 🔧 การทำงานของระบบ

#### 1. API Route
- **ไฟล์**: `app/api/upload-product-image/route.ts`
- **Endpoint**: `POST /stationaryhub/api/upload-product-image`
- **การทำงาน**:
  - รับไฟล์รูปภาพจาก FormData
  - ตรวจสอบประเภทไฟล์ (JPEG, PNG, WebP เท่านั้น)
  - ตรวจสอบขนาดไฟล์ (สูงสุด 5MB)
  - สร้างชื่อไฟล์ที่ไม่ซ้ำกัน: `product_[timestamp]_[random].[extension]`
     - บันทึกไฟล์ลงโฟลเดอร์ `public/` โดยตรง
  - ส่ง URL กลับไป

#### 2. การแสดงรูปภาพ
- **ฟังก์ชัน**: `getImageUrl()` ใน `app/admin/products/page.tsx`
- **การทำงาน**:
  - รองรับหลายรูปแบบของ URL
  - แสดงรูป placeholder ถ้าไม่มีรูปภาพ
  - แปลง path ให้ถูกต้องสำหรับการแสดงผล

### 📋 ขั้นตอนการใช้งาน

#### สำหรับ Admin:

1. **เข้าสู่หน้า Product Management**
   - ไปที่ `/stationaryhub/admin/products`

2. **เพิ่มสินค้าใหม่**
   - คลิกปุ่ม "Add New Product"
   - กรอกข้อมูลสินค้า

3. **อัปโหลดรูปภาพ**
   - คลิกปุ่ม "Upload Image" ในส่วนรูปภาพ
   - เลือกไฟล์รูปภาพ (JPEG, PNG, WebP)
   - รอการอัปโหลดเสร็จ
   - รูปภาพจะแสดงในส่วน preview

4. **บันทึกสินค้า**
   - คลิกปุ่ม "Add Product" หรือ "Update Product"
   - รูปภาพจะถูกบันทึกพร้อมกับข้อมูลสินค้า

### ⚙️ การตั้งค่า

#### ข้อจำกัดของไฟล์:
- **ประเภทไฟล์**: JPEG, JPG, PNG, WebP
- **ขนาดไฟล์**: สูงสุด 5MB
- **ชื่อไฟล์**: ระบบจะสร้างชื่อไฟล์อัตโนมัติ

#### โครงสร้างโฟลเดอร์:
```
public/
├── product_1756958886256_kuzp0wujndh.png
├── product_1755437688151_vo2lgf071k.png
├── placeholder.jpg
└── [รูปภาพอื่นๆ ที่มีอยู่แล้ว]
```

### 🔍 การแก้ไขปัญหา

#### ปัญหาที่อาจเกิดขึ้น:

1. **อัปโหลดไม่สำเร็จ**
   - ตรวจสอบประเภทไฟล์
   - ตรวจสอบขนาดไฟล์
   - ตรวจสอบสิทธิ์การเขียนไฟล์

2. **รูปภาพไม่แสดง**
   - ตรวจสอบ URL ในฐานข้อมูล
   - ตรวจสอบการตั้งค่า path ใน `getImageUrl()`

3. **โฟลเดอร์ไม่มี**
   - ระบบจะสร้างโฟลเดอร์อัตโนมัติ
   - ตรวจสอบสิทธิ์การสร้างโฟลเดอร์

### 📝 หมายเหตุ

- รูปภาพที่อัปโหลดจะถูกเก็บในโฟลเดอร์ `public` โดยตรง ซึ่งสามารถเข้าถึงได้จากภายนอก
- ไฟล์ `.gitignore` ในโฟลเดอร์ `public` จะป้องกันไม่ให้รูปภาพที่อัปโหลดถูกเก็บใน Git
- ระบบจะสร้างชื่อไฟล์ที่ไม่ซ้ำกันโดยอัตโนมัติ: `product_[timestamp]_[random].[extension]`
- รูปภาพจะถูกแสดงในตารางสินค้าและในหน้าแก้ไขสินค้า
- ควรระวังเรื่องความปลอดภัยของไฟล์ที่อัปโหลด
