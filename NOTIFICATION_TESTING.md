# 🔔 การทดสอบระบบการแจ้งเตือน (Notification System Testing)

## 📋 **ปัญหาที่แก้ไขแล้ว**

### ✅ **DialogContent Warning**
- แก้ไขโดยเพิ่ม `DialogTitle`, `DialogContent` และ `DialogDescription` ที่ถูกต้อง
- ใช้ Material-UI components ตามมาตรฐาน
- แก้ไขทั้งในหน้า Admin และ Approvals

### ✅ **API Error 500**
- แก้ไขการสร้างการแจ้งเตือนให้ใช้ `AdLoginName` แทน `USER_ID`
- ปรับปรุง API endpoint `/api/notifications/arrival`
- เพิ่ม debug logs และ error handling ที่ดีขึ้น
- เพิ่มการตรวจสอบการเชื่อมต่อฐานข้อมูล

## 🧪 **วิธีทดสอบระบบการแจ้งเตือน**

### **วิธีที่ 1: ใช้หน้า Test Notifications**
1. เข้าไปที่ `/test-notifications` (สำหรับ DEV role)
2. คลิกปุ่ม **"สร้างอัตโนมัติ"** เพื่อสร้างการแจ้งเตือนทดสอบ
3. ตรวจสอบไอคอนกระดิ่งใน header

### **วิธีที่ 2: ใช้ไฟล์ SQL**
1. รันไฟล์ `scripts/create-test-notifications.sql` ในฐานข้อมูล
2. รีเฟรชหน้าเว็บ
3. ตรวจสอบไอคอนกระดิ่ง

### **วิธีที่ 3: ใช้ปุ่มแจ้งเตือนใน Admin/Approvals**
1. เข้าไปที่หน้า Admin หรือ Approvals
2. คลิกปุ่ม **"แจ้งเตือน"** ในตาราง
3. กรอกข้อความและส่งการแจ้งเตือน

## 🔍 **การตรวจสอบ**

### **เปิด Developer Console (F12)**
- ดู console.log ที่เพิ่มไว้สำหรับ debug
- ตรวจสอบ Network tab สำหรับ API calls
- ดู debug logs จาก API `/api/notifications/arrival`

### **ตรวจสอบฐานข้อมูล**
```sql
-- ดูการแจ้งเตือนทั้งหมด
SELECT * FROM EMAIL_LOGS ORDER BY SENT_AT DESC

-- ดูการแจ้งเตือนของ user เฉพาะ
SELECT * FROM EMAIL_LOGS WHERE TO_USER_ID = 'admin' ORDER BY SENT_AT DESC

-- ดูการแจ้งเตือนทดสอบ
SELECT * FROM EMAIL_LOGS WHERE SUBJECT LIKE '%ทดสอบ%' OR SUBJECT LIKE '%สินค้ามาแล้ว%' OR SUBJECT LIKE '%อนุมัติ%'
```

## 📁 **ไฟล์ที่เกี่ยวข้อง**

- `components/Layout.tsx` - ไอคอนกระดิ่งและการแสดงผลการแจ้งเตือน
- `app/notifications/page.tsx` - หน้าประวัติการแจ้งเตือน
- `app/test-notifications/page.tsx` - หน้าทดสอบการแจ้งเตือน
- `app/admin/page.tsx` - หน้า Admin พร้อม Dialog ที่แก้ไขแล้ว
- `app/approvals/page.tsx` - หน้า Approvals พร้อม Dialog ที่แก้ไขแล้ว
- `app/api/notifications/route.ts` - API ดึงข้อมูลการแจ้งเตือน
- `app/api/notifications/arrival/route.ts` - API ส่งการแจ้งเตือนสินค้ามาแล้ว (แก้ไขแล้ว)
- `lib/notification-service.ts` - Service layer สำหรับการแจ้งเตือน

## 🚀 **ขั้นตอนการทดสอบ**

1. **รันไฟล์ SQL** เพื่อสร้างข้อมูลทดสอบ
2. **รีเฟรชหน้าเว็บ** และตรวจสอบไอคอนกระดิ่ง
3. **คลิกไอคอนกระดิ่ง** เพื่อดูรายการแจ้งเตือน
4. **ทดสอบปุ่มแจ้งเตือน** ในหน้า Admin/Approvals
5. **ตรวจสอบ console.log** สำหรับ debug information
6. **ตรวจสอบ Network tab** สำหรับ API calls

## 🔧 **การแก้ไขปัญหา**

### **DialogContent Warning**
- ใช้ `DialogDescription` component แทน `<p>` tag
- เพิ่ม `aria-describedby` attribute ถ้าจำเป็น
- ตรวจสอบให้แน่ใจว่า Dialog มี Description ที่ถูกต้อง

### **API Error 500**
- เพิ่ม debug logs เพื่อติดตามปัญหา
- ตรวจสอบการเชื่อมต่อฐานข้อมูล
- แก้ไขการจัดการข้อมูล user ที่อาจเป็น null
- เพิ่ม error handling ที่ดีขึ้น

## 💡 **หมายเหตุ**

- ระบบการแจ้งเตือนใช้ `AdLoginName` เป็น `TO_USER_ID` ในตาราง `EMAIL_LOGS`
- การแจ้งเตือนจะแสดงเฉพาะเมื่อ `STATUS = 'SENT'`
- สามารถลบการแจ้งเตือนได้โดยคลิกปุ่มลบในรายการ
- Debug logs จะแสดงใน console เมื่อใช้ปุ่มแจ้งเตือน

## �� **หากยังมีปัญหา**

1. **ตรวจสอบ console logs** ใน Developer Tools
2. **ตรวจสอบ Network tab** สำหรับ API errors
3. **ตรวจสอบฐานข้อมูล** ว่ามีข้อมูลที่ถูกต้องหรือไม่
4. **ตรวจสอบการเชื่อมต่อฐานข้อมูล** ใน server logs
